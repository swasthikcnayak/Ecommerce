spring:
  cloud:
    gateway:
      routes:
        ## service1
        - id: auth
          uri: lb://authservice
          predicates:
            - Path=/auth/**
          filters:
            - StripPrefix=1
